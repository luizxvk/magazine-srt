
function ZionsModalWrapper() {
  const { isZionsModalOpen, closeZionsModal } = useAuth();

  return (
    <ZionsPurchaseModal 
      isOpen={isZionsModalOpen} 
      onClose={closeZionsModal} 
    />
  );
}
